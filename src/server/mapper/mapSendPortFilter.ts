import SendPortState from '../parser/SendPortState';
import { SendPortFilter } from '../../shared/model';

const mapSendPortFilter = (sp: SendPortState): SendPortFilter => {
  throw new Error('not implemented');
};

export default mapSendPortFilter;
